<% @franchise.errors.each do |error| %>
  <%= error %>
<% end %> 

<%= form_for @franchise do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_area :description %>
  
  <%= f.submit %>

<% end %>
